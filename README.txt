Testing:
1. Make a folder named "test" in the same directory as "classify.py". 
2. Input images into the folder making certain ONLY .jpg images are within. 
3. Make certain that there is an already trained model named "model.p" in the same directory as "test" and "classify.py". 
4. Run "classify.py".

Make sure Python3.13 is set in Environmental Variables